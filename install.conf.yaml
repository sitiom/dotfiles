- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.config/nvim:
      path: nvim
      force: true
    ~/.zshrc:
      path: zsh/.zshrc
      force: true
    ~/.p10k.zsh:
        path: zsh/.p10k.zsh

- shell:
  - [git submodule update --init --recursive, Installing submodules]
