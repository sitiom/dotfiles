# Basic Settings
set shell pwsh
set hidden off
set incsearch on
set drawbox true
set icons true
set info size:time
set tabstop 4

# Mappings
map D delete
map x cut

cmd open &start $Env:f
map e $&$Env:EDITOR $Env:f
map i $Invoke-Expression "$Env:PAGER $Env:f"
map w $&$Env:SHELL
